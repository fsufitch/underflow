package master

import "context"

// Client encapsulates an Underflow Master running in client (reverse) mode
type Client struct{}

// Run starts an Underflow Master in client (reverse) mode
func (c Client) Run(ctx context.Context) error {
	// TODO
	return nil
}
